# Reload ESLint Config

__Writing JavaScript, the Reload way.__

## Install

```bash
npm install eslint @reloaddk/eslint-config-reload
```

## Usage

Add this to your `.eslintrc` file:

```json
{
  "extends": "@reloaddk"
}
```
